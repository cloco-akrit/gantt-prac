# Chart

#### Setup
- Verify node.js version
    ```bash
    $ node --version
    v18.19.0
    ```

- Install dependencies
    ```bash
    $ yarn install
    ```

- Start server
    ```bash
    $ yarn dev
    ```